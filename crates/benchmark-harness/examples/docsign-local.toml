# Benchmark config for docsign-web (local)

[benchmark]
name = "DocSign Local Performance"
base_url = "http://127.0.0.1:8081"
iterations = 10
warmup = 2
parallel_contexts = 2

[throttling]
network_profile = "None"
cpu_slowdown = 1.0

[thresholds]
lcp_p95 = 3000.0
inp_p95 = 200.0
cls_p95 = 0.1

[[scenarios]]
name = "Homepage Load"
steps = [
    { type = "navigate", url = "/" },
    { type = "wait", wait_for = "network_idle" },
    { type = "measure", label = "homepage" }
]

[[scenarios]]
name = "WASM Load"
steps = [
    { type = "navigate", url = "/" },
    { type = "wait", wait_for = "selector", selector = ".drop-zone" },
    { type = "measure", label = "wasm-ready" }
]
