{
  "name": "docsign-tauri",
  "version": "0.1.0",
  "description": "GetSignatures desktop application for document signing",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:debug": "tauri build --debug",
    "typecheck": "tsc --noEmit",
    "sync-frontend": "rsync -av --delete ../docsign-web/www/ ./src/",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/node": "^22.0.0",
    "fast-check": "^3.23.0",
    "typescript": "^5.0.0",
    "vitest": "^2.1.0"
  }
}
